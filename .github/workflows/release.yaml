name: Release

on:
  push:
    tags:
      - "v*"

permissions: {}

jobs:
  release:
    uses: luxass/shared-workflows/.github/workflows/release-npm.yaml@f679de59851660fb23fd7e453ea66b9e76cb55aa # v0.11.0
    permissions:
      contents: write
      id-token: write
    with:
      environment: npm-registry
